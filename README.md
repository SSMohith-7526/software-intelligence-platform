# Software Intelligence Platform

This archive contains the requested folder structure only.

The complete implementation (FastAPI backend, React frontend, agent logic, orchestration, AST/CFG generation, runtime simulation, etc.) is too large to generate accurately in a single chat response.

Use this structure as the project scaffold in VS Code.
